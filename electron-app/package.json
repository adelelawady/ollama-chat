{
  "name": "electron-app",
  "version": "1.0.0",
  "description": "A basic Electron application with React frontend",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "node scripts/build.js",
    "dev": "npm run build && electron .",
    "clean": "rimraf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "electron",
    "desktop",
    "react"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.0.0",
    "rimraf": "^5.0.10"
  }
}
